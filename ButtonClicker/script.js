function logout(element){
    if(element.innerText == "Login"){
        element.innerText = "Logout";
    }
}

function like(){
    alert('Ninja was liked');
}

function addDefination(element){
   element.remove();
}